# Intro to OpenSearch in GenAI

We're going to dive into the specifics of OpenSearch next. OpenSearch is an important backend vector store that is used with Bedrock Knowledge Bases, so it's important to have the specifics on how it works at a general level in addition to GenAI-specific guidance.

So, let's start with some general info and tuning advice for OpenSearch... then we'll come back with some Bedrock-specific and AI-specific guidance for using OpenSearch as a vector store.



Involving S3 document repositories together with RDS, but before I get into that, just remember that RDS can serve as a vector store, so there is an RDS SQL Server vector store that's also an option for storing your vector embedding values there. Now, one pattern that the guide calls out is using RDS for querying your structured data, and then that in turn points to something like S3 for large unstructured data that it might refer to. So one idea is to use RDS as kind of a catalog and the for the metadata for stuff that's stored in an unstructured manner in S3 and with really, really large bits of data. So one thing you can do is you could use a vector store backed by RDS for your rag system, and then what you get back from that vector store is actually just pointing you to content in S3. So the idea here is that RDS is gonna be my vector store, and instead of just returning the actual data itself that that vector is associated with, it's gonna be returning back a pointer to S3 where you get the actual data from. So that is one pattern you should be aware of in the back of your head, using RDS as a vector store backed by an S3 document repository where it's pointing to data in S3.


